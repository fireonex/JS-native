let myMap = {
    'apple': 2,
    'banana': 1
};

updateMapValue(myMap, 'apple');
updateMapValue(myMap, 'orange');

export function updateMapValue(map: Record<string, number>, key: string): void {
    
}
